import { StyleSheet, Text, View } from 'react-native';

function AccountView() {
  return (
    <View style={styles.container}>
      <Text>Placeholder for AccountView</Text>
    </View>
  );
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
  },
});

export default AccountView;
