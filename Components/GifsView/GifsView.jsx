import { StyleSheet, Text, View } from 'react-native';

function GifsView() {
  return (
    <View style={styles.container}>
      <Text>Placeholder for GifsView</Text>
    </View>
  );
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
  },
});

export default GifsView;
